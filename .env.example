# Origin of the chat backend (backend/ in this repo, deployed separately).
# Public by design — it is a URL, not a secret. The Gemini key lives server-side.
PUBLIC_API_BASE=https://portfolio-backend-virid-seven.vercel.app
